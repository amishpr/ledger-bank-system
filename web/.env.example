VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000/ws

# Set to true to run the in-browser demo backend instead of calling the
# API above. `npm run dev:demo` sets it for you, and netlify.toml sets it
# for the hosted build, so it normally stays unset here.
# VITE_DEMO=true
